// In file: frontend/next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  // This tells Next.js to ignore TypeScript errors during the production build.
  typescript: {
    ignoreBuildErrors: true,
  },
};

module.exports = nextConfig;    